name: 'build-test'
on: # rebuild any PRs and main branch changes
  pull_request:
  push:
    branches:
      - main
      - 'releases/*'

jobs:
  build: # make sure build/ci work properly
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          npm install
      - run: |
          npm run all
  test: # make sure the action works on a clean machine without building
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          ctpUrl: 'http://104.42.225.105/em'
          ctpUsername: 'admin'
          ctpPassword: 'admin'
          ctpJob: 'Status Job'
          publishReport: 'true'
          dtpUrl: 'http://54.149.98.67:8080'
          dtpUsername: 'demo'
          dtpPassword: 'demo-user'
          dtpProject: 'Parabank-v3'
          buildId: 'Parabank-v3-2021-01-21'
          sessionTag: 'Parabank-v3-Session'
          appendEnvironment: 'true'
